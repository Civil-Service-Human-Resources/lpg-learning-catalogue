test:
  integration:
    elastic:
      useLocalElastic: ${INTEGRATION_TEST_USE_LOCAL_ELASTIC:false}
      elasticImage: "docker.elastic.co/elasticsearch/elasticsearch:8.4.2"
logging:
  level:
    root: DEBUG
