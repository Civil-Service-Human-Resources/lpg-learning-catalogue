
spring:
  resources:
    add-mappings: false

server:
  port: 9001

logging:
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  level:
    root: ${ROOT_LOGGING_LEVEL:INFO}
    com.zaxxer.hikari: ERROR

elasticsearch:
  uri: http://127.0.0.1:9200
  username: elastic
  password: changeme
  host: 127.0.0.1
  port: 9999

azure:
  account:
    name: ${AZURE_ACCOUNT_NAME:accountName}
    key: ${AZURE_ACCOUNT_KEY:cGFzc3dvcmQ=}
  storage.connection: "DefaultEndpointsProtocol=https;AccountName=${azure.account.name};AccountKey=${azure.account.key}"